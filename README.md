The script download.py downloads the brain observatory dataset and produces figures 3 and 4.

running
-------
create a python virtual environment, activate it, and then
```
pip install -r requirements.txt && download.py
```
This will download the dataset to your current directory (this may take a while), and generate the figures.

